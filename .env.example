# Payload CMS Configuration
PAYLOAD_SECRET=your-secret-key-here
DATABASE_URI=mongodb://localhost:27017/cplab

# Development
NODE_ENV=development
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload - Vercel Blob Storage
# Get this token from: https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=your-vercel-blob-token-here

# Authentication
JWT_SECRET=your-jwt-secret-here

# Vercel Deployment Notes:
# 1. Set all these environment variables in Vercel Dashboard > Settings > Environment Variables
# 2. For BLOB_READ_WRITE_TOKEN: Go to Vercel Dashboard > Storage > Create Blob Store > Copy token
# 3. For DATABASE_URI: Use MongoDB Atlas or your preferred MongoDB service
# 4. Make sure to set NODE_ENV=production for production deployment

# Optional: S3 Configuration for file storage
S3_ENDPOINT=
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
